// Create an array of strings and use forEach to log each string in uppercase to the console.
 var data =["apple", "banana", "cherry"];

 data.forEach(function (element,index) {
    
    
    console.log(element.toUpperCase());  // using toUpperCase() method to make string uppercase
    
    
 })