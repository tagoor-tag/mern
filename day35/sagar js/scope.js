// this is the gobal scope that can use any where in the file
//but it can used in another js file

function fn(){
    //this is function scope that can be used in function only 
    //but not in another where
}

if(a){
    // this is a block scope that can be used in block only
    //but not in another where
}